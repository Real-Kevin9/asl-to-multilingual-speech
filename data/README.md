# Data Directory

This folder stores project data and generated artifacts.

## Guidelines
- Keep raw datasets in `data/raw/`.
- Keep intermediate outputs in `data/processed/` and `data/augmented/`.
- Do not commit personal or sensitive recordings.
- Use anonymized data for experiments and user studies.
